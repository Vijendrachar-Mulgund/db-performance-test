const config = {
  db: {
    user: "VALUE", // Your PostgreSQL username
    host: "VALUE", // Database server address
    database: "VALUE", // Name of the database
    password: "VALUE", // Your PostgreSQL password
    port: "VALUE",
  },
};

module.exports = config;
